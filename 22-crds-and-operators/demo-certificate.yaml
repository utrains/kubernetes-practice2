apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: demo-tls
  namespace: default
spec:
  secretName: demo-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  commonName: demo.example.com
  dnsNames:
    - demo.example.com
